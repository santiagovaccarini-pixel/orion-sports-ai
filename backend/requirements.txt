fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
psutil>=7.0,<8.0
psycopg[binary,pool]==3.2.10
# Uploads arrive as multipart so a PDF or a workbook can be sent as bytes;
# FastAPI needs this to parse that body at all.
python-multipart==0.0.20
# The formats a sports professional actually receives: GPS exports as Excel,
# medical reports as PDF, scouting summaries as Word. All read-only parsers.
pypdf==6.1.1
openpyxl==3.1.5
python-docx==1.1.2
